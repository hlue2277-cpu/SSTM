<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SSTMTerminal.Styles"
                    xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
                    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                    xmlns:controls="clr-namespace:SSTMTerminal.Controls">
    <Style x:Key="{x:Type controls:TipTextBox}" TargetType="{x:Type controls:TipTextBox}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="CaretBrush" Value="#6371a1" />
        <Setter Property="Foreground" Value="#6371a1" />
        <Setter Property="Width" Value="238" />
        <Setter Property="Height" Value="42" />
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:TipTextBox}">
                    <Grid Background="Transparent">
                        <TextBlock x:Name="tipTextBlock"
                                   Margin="35,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="#6371a1"
                                   Text="{TemplateBinding TipText}"
                                   Visibility="Collapsed" />
                        <Border x:Name="Border"
                                Margin="2"
                                Padding="2"
                                BorderBrush="#c2c2c2"
                                BorderThickness="2"
                                CornerRadius="6">
                            <StackPanel Orientation="Horizontal">
                                <Image MaxHeight="20"
                                       Margin="5,0,0,0"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       Source="{TemplateBinding Icon}"
                                       Stretch="Uniform" />
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="5,0,0,0"
                                              VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                        <!--<Border x:Name="outerBorder"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                BorderBrush="#6371a1"
                                BorderThickness="2"
                                CornerRadius="3"
                                Opacity="0.3"
                                Visibility="Collapsed" />-->
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="tipTextBlock" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="tipTextBlock" Property="Visibility" Value="Collapsed" />
                            <!--<Setter TargetName="outerBorder" Property="Visibility" Value="Visible" />-->
                            <Setter TargetName="Border" Property="BorderBrush" Value="#7629cc6d" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>